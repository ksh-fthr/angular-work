import { NetworkAddressValidatorDirective } from './network-address-validator.directive';

describe('NetworkAddressValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NetworkAddressValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
